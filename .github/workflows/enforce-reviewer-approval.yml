name: Enforce Reviewer Approvals

on:
  pull_request:
    types: [opened, review_requested, review_request_removed, synchronize]
  pull_request_review:
    types: [submitted, edited, dismissed]

permissions:
  pull-requests: write
  checks: write
  actions: write

jobs:
  clear-old-runs:
    runs-on: ubuntu-latest
    steps:
      - name: Fetch & Invalidate Previous Runs
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const { owner, repo } = context.repo;
            const currentRunId = context.runId;
            const branch = context.payload.pull_request ? context.payload.pull_request.head.ref : null;
            
            if (!branch) {
              console.log("⚠️ Not a PR event, skipping cancellation.");
              return;
            }

            console.log(`🔄 Fetching all previous workflow runs on branch: ${branch}...`);

            let page = 1;
            let perPage = 100;
            let allRuns = [];

            while (true) {
              const { data: runs } = await github.rest.actions.listWorkflowRunsForRepo({
                owner,
                repo,
                per_page: perPage,
                page: page
              });

              if (runs.workflow_runs.length === 0) break;
              allRuns = allRuns.concat(runs.workflow_runs);
              page++;
            }

            let invalidated = 0;
            for (const run of allRuns) {
              if (
                run.id !== currentRunId &&
                run.head_branch === branch
              ) {
                console.log(`✅ Marking previous run as successful: ${run.id}`);
                await github.rest.checks.create({
                  owner,
                  repo,
                  name: "Enforce Reviewer Approvals",
                  head_sha: run.head_sha,
                  status: "completed",
                  conclusion: "success",
                  output: {
                    title: "Invalidated Old Runs",
                    summary: "Old stale workflow runs have been cleared."
                  }
                });
                invalidated++;
              }
            }

            console.log(invalidated > 0 
              ? `✅ Invalidated ${invalidated} old runs.` 
              : "✅ No previous runs found to invalidate.");

  enforce-approvals:
    runs-on: ubuntu-latest
    needs: clear-old-runs
    steps:
      - name: Check Required Approvals
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const prNumber = context.payload.pull_request.number;
            const owner = context.repo.owner;
            const repo = context.repo.repo;

            // Fetch all requested reviewers
            const { data: requestedReviewers } = await github.rest.pulls.listRequestedReviewers({
              owner,
              repo,
              pull_number: prNumber
            });

            const requiredReviewers = new Set(requestedReviewers.users.map(user => user.login));

            // Fetch all reviews
            const { data: reviews } = await github.rest.pulls.listReviews({
              owner,
              repo,
              pull_number: prNumber
            });

            // Fetch timeline events (track re-review requests)
            const { data: timeline } = await github.rest.issues.listEventsForTimeline({
              owner,
              repo,
              issue_number: prNumber
            });

            const reRequestedReviewers = new Set();
            for (const event of timeline) {
              if (event.event === "review_requested" && event.requested_reviewer) {
                reRequestedReviewers.add(event.requested_reviewer.login);
              }
            }

            // Track latest review states
            const latestReviews = new Map();
            for (const review of reviews) {
              latestReviews.set(review.user.login, review.state);
            }

            // Check if all required reviewers have approved
            let allApproved = true;
            for (const reviewer of requiredReviewers) {
              if (reRequestedReviewers.has(reviewer) || latestReviews.get(reviewer) !== "APPROVED") {
                allApproved = false;
                break;
              }
            }

            if (allApproved) {
              console.log("✅ All required reviewers have approved.");
            } else {
              console.log("❌ Not all required reviewers have approved.");
              core.setFailed("Pull request cannot be merged until all requested reviewers approve.");
            }

      - name: Force Re-Run After Clearing Old Runs
        if: success()
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const { owner, repo } = context.repo;
            console.log("🔄 Triggering a fresh workflow run...");
            await github.rest.actions.createWorkflowDispatch({
              owner,
              repo,
              workflow_id: "enforce-review-approvals.yml",
              ref: context.ref
            });
